# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Local source artwork — the launcher icon is embedded in the userscript,
# so the full-size master doesn't need to live in the repo.
icon.png
